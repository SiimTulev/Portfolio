
@{
    Layout = null;
    //Layout = "~/Views/Shared/EnableBlazor.cshtml";
}

<head>
    <script src="~/lib/jquery/dist/jquery.js"></script>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.1/jquery.min.js"></script>

    <meta charset="utf-8" />
    <meta name="viewport" content="width=device=width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link rel="stylesheet" href="~/css/green.css" />
    <title>GSAP</title>
</head>
<body>

    @*<div class="box green"></div>
        <div class="box orange"></div>
        <div class="box grey"></div>*@

    <h1>Engineering For Tomorrow</h1>
    <button id="cta">watch here</button>

    <div class="panel">
        <img src="~/images/greenRing.jpg" id="light" />
        <p> oooo so so so nicee veery good cool yee  very nice good cool stuff yeah ok cya</p>
        <div class="box"></div>
    </div>

</body>

<script src="https://cdnjs.cloudflare.com/ajax/libs/gsap/3.5.1/gsap.min.js"></script>

<script>

$(window).load(function () {
    @* Loading effect *@
    $(".loader-wrapper").fadeOut("slow");

    //gsap.to(".box", { rotation: 27, x: 100, duration: 1 });
    TweenMax.to('.panel', 1.5, { scaleY: 1, height: '100vh', ease: Circ.easeOut });

    gsap.to('#light', { duration: 5, opacity: 1, y: 0, delay: 1, ease: "elastic.out(1, 1)" });

    gsap.to('p', { duration: 2, opacity: 1, y: 20, delay: 1.4 });
    gsap.to('.box', { duration: 3, opacity: 1, scale: 1, ease: "elastic.out(1, 1)"  })

        // TODO TUTORIAL : https://www.youtube.com/watch?v=rJRrUHds7fc&ab_channel=GreenSockLearning
        // So I can use custom EASE

});

        document.getElementById('cta').addEventListener('click', function () {

            //TweenMax.to('.panel', 1.5, { scaleY: 1, height: '100vh', });
            //gsap.to('#light', 2,{  opacity: 1, y: 0, delay: 1, ease: "Bounce.easeOut" });
    })

    //document.getElementById("cta").addEventListener('click', function ()  {
    //    gsap.to('.panel', {
    //        duration: 1.5,
    //        ease: Power2.easeOut,
    //        delay: 2
    //    });
    //});


</script>


<style>

    body {
        margin: 10px;
    }

    .box {
        display: block;
    }

    .pink {
        background-color: pink;
    }
</style>